# OS
.DS_Store
Thumbs.db

# Logs & temp
*.log
.vscode/
.idea/
.vercel
.env*

# Agent worktrees — transient working copies created by the build team,
# never project content. Each is a full checkout; committing one would
# duplicate the entire repository.
.claude/worktrees/

# Portal demo harness output: seeded credentials (demo password, TOTP secret),
# record ids and responsive screenshots. Local only; regenerated by
# `pnpm demo:seed` and `pnpm screens` in app/.
app/.demo/
